// Geovisto 
import { ILayerToolConfig } from "geovisto";

/**
 * This type provides specification of the tiles layer tool config model.
 * 
 * @author Jiri Hynek
 */
type IImageLayerTool = ILayerToolConfig
export default IImageLayerTool;